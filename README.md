# Проект по генерации и анализу текста

## Установка и запуск

1. Создайте виртуальное окружение:
   ```
   python -m venv venv
   ```
или 
   ```
   py -m venv venv
   ```

2. Активируйте виртуальное окружение:
   - Для Windows:
     ```
     venv\Scripts\activate
     ```
   - Для macOS и Linux:
     ```
     source venv/bin/activate
     ```

3. Установите зависимости:
   ```
   pip install -r requirements.txt
   ```

4. Запуск проекта:
   - Для генерации текста:
     ```
     python text_generation.py
     ```
   - Для анализа текста:
     ```
     python text_analysis.py
     ```
   - Для обучения модели:
     ```
     python model_training.py
     ```

## Файлы проекта

- `text_generation.py`: Скрипт для генерации текста
- `text_analysis.py`: Скрипт для анализа текста
- `model_training.py`: Скрипт для обучения модели
- `requirements.txt`: Список зависимостей проекта
 
users.json - файл с пользователями 
 запускать надо файл main.py
 в папке sample_data датасеты 
# match_kursovay
